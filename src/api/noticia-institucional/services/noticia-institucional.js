'use strict';

/**
 * noticia-institucional service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::noticia-institucional.noticia-institucional');
